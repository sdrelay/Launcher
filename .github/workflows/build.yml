name: Build ts3server Launcher

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true   # Removes the warning
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install build tools
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc build-essential
    
    - name: Compile launcher
      run: |
        gcc -static -o ts3server launcher.c
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ts3server
        path: ts3server
